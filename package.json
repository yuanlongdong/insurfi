{
  "name": "insurfi-contracts",
  "version": "1.0.0",
  "description": "InsurFi - BSC Chain Trading Loss Insurance DeFi Protocol",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network bsctestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network bscmainnet",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "@openzeppelin/contracts": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.0"
  }
}
